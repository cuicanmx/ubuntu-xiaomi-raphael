# Enhanced workflow for building kernel and rootfs with multi-distro support

name: Build Raphael RootFS

permissions:
  contents: write

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Kernel version and release tag (e.g., 6.18)'
        required: true
        type: string
        default: '6.18'
      distribution:
        description: 'Distribution to build (ubuntu/armbian)'
        required: true
        default: 'ubuntu'
        type: choice
        options:
          - ubuntu
          - armbian
      version:
        description: 'Distribution version'
        required: true
        default: 'noble'
        type: choice
        options:
          - noble
          - jammy
          - focal
      kernel_source:
        description: 'Source of kernel packages (release only)'
        required: true
        default: 'release'
        type: choice
        options:
          - release
      desktop_environment:
        description: 'Desktop environment to install (only for ubuntu)'
        required: false
        default: 'none'
        type: choice
        options:
          - none
          - xfce
          - gnome
          - kde

env:
  KERNEL_VERSION: ${{ github.event.inputs.kernel_version }}
  DISTRIBUTION: ${{ github.event.inputs.distribution }}
  KERNEL_SOURCE: ${{ github.event.inputs.kernel_source }}
  RELEASE_TAG: v${{ github.event.inputs.kernel_version }}
  DESKTOP_ENVIRONMENT: ${{ github.event.inputs.desktop_environment }}

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # Build root filesystem
  rootfs:
    name: Build RootFS
    runs-on: ubuntu-24.04-arm  
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup environment variables
      run: |
        echo "Building rootfs for distribution: $DISTRIBUTION"
        echo "Kernel version: $KERNEL_VERSION"
        echo "Kernel source: $KERNEL_SOURCE"
        echo "Release tag: $RELEASE_TAG"
        echo "Desktop environment: $DESKTOP_ENVIRONMENT"
        
    - name: Setup inputs
      run: |
        echo "🚀 Starting rootfs build"
        echo "📦 Distribution: $DISTRIBUTION"
        echo "🔧 Kernel Version: $KERNEL_VERSION"
        echo "📎 Kernel Source: $KERNEL_SOURCE"
        if [[ "$KERNEL_SOURCE" == "release" ]]; then
          echo "🏷️  Release Tag: $RELEASE_TAG"
        fi
         
    - name: Download kernel packages from GitHub Release
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        echo "📥 Downloading kernel packages from GitHub Release: $RELEASE_TAG"
        
        # Download compressed kernel archive only
        echo "🔍 Downloading compressed kernel archive..."
        gh release download $RELEASE_TAG \
          --pattern "kernel-*-raphael.tar.gz" \
          --pattern "kernel-*-raphael.zip" \
          --repo ${{ github.repository }}
        
        # Extract the compressed archive
        tar_file=$(find . -name "kernel-*-raphael.tar.gz" -type f | head -1)
        zip_file=$(find . -name "kernel-*-raphael.zip" -type f | head -1)
        
        if [ -f "$tar_file" ]; then
          echo "📦 Extracting tar.gz archive: $tar_file"
          tar -xzf "$tar_file"
          echo "✅ tar.gz archive extracted successfully"
        elif [ -f "$zip_file" ]; then
          echo "📦 Extracting zip archive: $zip_file"
          unzip -q "$zip_file"
          echo "✅ zip archive extracted successfully"
        else
          echo "❌ No compressed kernel archive found"
          echo "📦 Available files in release:"
          gh release view $RELEASE_TAG --repo ${{ github.repository }} --json assets --jq '.assets[].name'
          exit 1
        fi
        
        # Verify extracted packages
        echo "🔍 Verifying extracted kernel packages:"
        
        # Use wildcard pattern to match actual package names with full version
        REQUIRED_PACKAGES=("linux-xiaomi-raphael_*_arm64.deb" 
                          "firmware-xiaomi-raphael_*_arm64.deb"
                          "alsa-xiaomi-raphael_*_arm64.deb")
        
        for pkg_pattern in "${REQUIRED_PACKAGES[@]}"; do
          # Find the actual package file matching the pattern
          pkg_file=$(find . -name "$pkg_pattern" -type f | head -1)
          
          if [ -f "$pkg_file" ]; then
            echo "✅ Found: $pkg_file"
            dpkg-deb -I "$pkg_file" || echo "⚠️ Package verification failed for $pkg_file"
          else
            echo "❌ Missing package matching pattern: $pkg_pattern"
            echo "📁 Available files:"
            ls -la *.deb 2>/dev/null || echo "No .deb files found"
            exit 1
          fi
        done
        
    # 删除artifacts获取方式，只保留GitHub Releases作为内核包来源
    - name: Install rootfs build dependencies
      run: |
         echo "📦 Installing rootfs build dependencies..."
         sudo apt update
         
         # 基础依赖包
         BASE_DEPS="fakeroot cpio kmod rsync wget curl file parted dosfstools e2fsprogs util-linux systemd-container jq kpartx"
         
         # 在ARM64架构上安装依赖
         echo "🔧 Running on ARM64, installing native dependencies..."
         sudo apt install -y binfmt-support $BASE_DEPS

    - name: Build rootfs
      run: |
         set -e  # Exit on any error
         echo "🚀 Starting rootfs build for distribution: $DISTRIBUTION"
         echo "📦 Kernel version: $KERNEL_VERSION"
         echo "🖥️  Desktop environment: $DESKTOP_ENVIRONMENT"
         echo "📥 Kernel source: $KERNEL_SOURCE"
         
         # Set environment variables for the build script
         export KERNEL_VERSION="$KERNEL_VERSION"
         export DISTRIBUTION="$DISTRIBUTION"
         export DESKTOP_ENVIRONMENT="$DESKTOP_ENVIRONMENT"
         export KERNEL_SOURCE="$KERNEL_SOURCE"
         export GITHUB_WORKSPACE="$GITHUB_WORKSPACE"
         
         # Build rootfs with error handling
         if ! sudo bash ./raphael-rootfs_build.sh; then
           echo "❌ RootFS build failed!"
           exit 1
         fi
         echo "✅ RootFS build completed successfully for $DISTRIBUTION"

    - name: Validate rootfs build artifacts
      run: |
         # Validate rootfs build artifacts
         ROOTFS_IMAGE="root-${DISTRIBUTION}-${KERNEL_VERSION}.img"
         [ -f "$ROOTFS_IMAGE" ] || { echo "Missing rootfs image file: $ROOTFS_IMAGE"; exit 1; }
         [ -s "$ROOTFS_IMAGE" ] || { echo "RootFS image is empty or invalid"; exit 1; }
         
         # Mount and verify content
         sudo kpartx -a "$ROOTFS_IMAGE"
         sleep 2
         
         if [ -b /dev/mapper/loop0p2 ]; then
           sudo mkdir -p /mnt/rootfs
           sudo mount /dev/mapper/loop0p2 /mnt/rootfs
           
           # Check key files
           key_files=("/mnt/rootfs/etc/hostname" "/mnt/rootfs/etc/fstab" "/mnt/rootfs/boot/vmlinuz*" "/mnt/rootfs/lib/modules")
           for file in "${key_files[@]}"; do
             [ -e "$file" ] || echo "Missing: $file"
           done
           
           # Check kernel modules
           ls /mnt/rootfs/lib/modules/$KERNEL_VERSION* 1> /dev/null 2>&1 || echo "Kernel modules not found"
           
           sudo umount /mnt/rootfs
           sudo rmdir /mnt/rootfs
         fi
         
         sudo kpartx -d "$ROOTFS_IMAGE"

    - name: Compress rootfs image to zip
      run: |
         ROOTFS_IMAGE="root-${DISTRIBUTION}-${KERNEL_VERSION}.img"
         ZIP_FILE="root-${DISTRIBUTION}-${KERNEL_VERSION}.zip"
         
         zip -9 "$ZIP_FILE" "$ROOTFS_IMAGE"
         unzip -t "$ZIP_FILE" > /dev/null 2>&1 || { echo "Zip file integrity check failed"; exit 1; }
         
    - name: Prepare release files
      run: |
        echo "📁 Preparing release-files directory"
        rm -rf release-files || true
        mkdir -p release-files
        # Copy kernel packages from output/kernel directory
        if [ -d "output/kernel" ]; then
          cp -r output/kernel/* release-files/ || true
        fi
        # Copy DEBs and rootfs artifacts into release-files preserving structure
        find . -type f \( -name "*.deb" -o -name "*.7z" -o -name "*.zip" -o -name "*.img" -o -name "*.tar.gz" \) -exec cp --parents {} release-files/ \; || true
        echo "📋 Release files:"
        ls -la release-files || true
        echo "� Release files structure:"
        find release-files -type f | head -20 || true

    - name: Upload to GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.RELEASE_TAG }}
        name: "🎯 小米 Raphael ${{ github.event.inputs.distribution }} 发行版 - 内核 ${{ github.event.inputs.kernel_version }}"
        body: |
          # 🎯 小米 Raphael (K20 Pro) ${{ github.event.inputs.distribution }} 发行版
          
          ## 📦 发行版信息
          
          ### 🏷️ 基本信息
          - **设备型号**: 小米 Raphael (K20 Pro)
          - **发行版本**: ${{ github.event.inputs.distribution }}
          - **内核版本**: ${{ github.event.inputs.kernel_version }}
          - **桌面环境**: ${{ github.event.inputs.desktop_environment }}
          - **架构平台**: ARM64
          
          ### 📊 构建详情
          - **构建日期**: ${{ github.event.repository.created_at }}
          - **构建ID**: ${{ github.run_id }}
          - **发布标签**: ${{ env.RELEASE_TAG }}
          
          ## 🗂️ 文件说明
          
          ### 🔧 RootFS 系统镜像
          - **完整镜像**: root-${{ github.event.inputs.distribution }}-${{ github.event.inputs.kernel_version }}.img
          - **压缩版本**: root-${{ github.event.inputs.distribution }}-${{ github.event.inputs.kernel_version }}.zip (推荐用于启动镜像构建)
          
          ### 🚀 启动文件包
          - **启动包**: boot-files-${{ github.event.inputs.distribution }}-${{ github.event.inputs.kernel_version }}.zip
            - 包含 UUID、initrd.img 和 vmlinuz 文件
            - 用于启动镜像创建
          
          ## 📋 使用指南
          
          ### 🔥 刷机说明
          ```bash
          # 解压 RootFS 镜像
          unzip root-${{ github.event.inputs.distribution }}-${{ github.event.inputs.kernel_version }}.zip
          
          # 刷入设备 (根据您的设置调整)
          fastboot flash system root-${{ github.event.inputs.distribution }}-${{ github.event.inputs.kernel_version }}.img
          ```
          
          ### 💡 使用提示
          - 启动镜像构建时，建议下载 .zip 文件以获得更快的传输速度
          - 使用前请从 .zip 文件中提取 .img 镜像文件
          - 确保设备已解锁 bootloader
          - 建议在刷机前备份重要数据
          
          ## 🎯 设备兼容性
          
          ### ✅ 支持设备
          - 小米 Raphael (K20 Pro)
          - 基于 SM8150 平台的设备
          
          ### 🔧 硬件要求
          - ARM64 架构处理器
          - 至少 2GB 可用存储空间
          - 已解锁的 bootloader
          
          ## 📝 更新日志
          
          ### 🔄 版本更新
          - **内核版本**: ${{ github.event.inputs.kernel_version }}
          - **发行版本**: ${{ github.event.inputs.distribution }}
          - **桌面环境**: ${{ github.event.inputs.desktop_environment }}
          
          ### ✨ 功能特性
          - 完整的 Linux 发行版体验
          - 优化的 ARM64 性能
          - 针对小米 Raphael 设备的专门优化
          - 稳定的内核和系统组件
          
          ## ❓ 常见问题
          
          ### Q: 如何验证镜像完整性？
          A: 下载后检查文件大小和哈希值，确保文件完整。
          
          ### Q: 是否需要特殊工具？
          A: 需要 fastboot 工具和已解锁的 bootloader。
          
          ### Q: 支持哪些桌面环境？
          A: 当前支持 ${{ github.event.inputs.desktop_environment }} 桌面环境。
          
          ---
          
          *💫 由 GitHub Actions 自动构建，专为小米 Raphael 设备优化*
        files: |
          release-files/**