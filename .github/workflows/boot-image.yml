name: Boot Image Creation

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Kernel version for boot image (e.g., 6.18)'
        required: true
        type: string
        default: '6.18'
      distribution:
        description: 'Distribution to use (ubuntu/armbian)'
        required: true
        type: string
        default: 'ubuntu'

env:
  KERNEL_VERSION: ${{ github.event.inputs.kernel_version }}
  DISTRIBUTION: ${{ github.event.inputs.distribution }}
  RELEASE_TAG: v${{ github.event.inputs.kernel_version }}

jobs:
  create-boot-image:
    name: Create Boot Image
    runs-on: ubuntu-24.04-arm
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup environment
      run: |
        echo "🔧 Creating boot image for $DISTRO with kernel $KERNEL_VERSION"
        echo "🏷️ Release tag: $RELEASE_TAG"
        
    - name: Cache build components
      uses: actions/cache@v3
      with:
        path: |
          ~/.cache
          ./boot-image-build
          ./rootfs-*.*
        key: ${{ runner.os }}-boot-image-${{ github.event.inputs.kernel_version }}-${{ github.event.inputs.distribution }}-${{ hashFiles('build-config.sh', 'raphael-boot_build.sh') }}
        restore-keys: |
          ${{ runner.os }}-boot-image-${{ github.event.inputs.kernel_version }}-${{ github.event.inputs.distribution }}-
          ${{ runner.os }}-boot-image-${{ github.event.inputs.kernel_version }}-
          ${{ runner.os }}-boot-image-
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          wget \
          curl \
          parted \
          dosfstools \
          e2fsprogs \
          mtools \
          qemu-user-static \
          binfmt-support \
          fakeroot \
          cpio \
          kmod \
          rsync \
          jq
          
    - name: Download rootfs and boot packages
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        echo "📥 Downloading rootfs and boot packages..."
        
        # 下载boot包（包含UUID、initrd.img、vmlinuz）
        echo "📦 Downloading boot package..."
        gh release download $RELEASE_TAG \
          --pattern "boot-files-*$DISTRIBUTION*.zip" \
          --repo ${{ github.repository }} || {
          echo "⚠️ Failed to download boot package from release, trying artifacts"
          # 从工作流产物下载
          gh run download --name "xiaomi-raphael-rootfs_${DISTRIBUTION}_${KERNEL_VERSION}" || {
            echo "❌ No boot package found"
            exit 1
          }
        }
        
        # 解压boot包
        BOOT_PACKAGE=$(ls boot-files-*.zip | head -1)
        if [ -n "$BOOT_PACKAGE" ]; then
          echo "🗜️ Extracting boot package: $BOOT_PACKAGE"
          unzip "$BOOT_PACKAGE"
          echo "✅ Boot package extracted"
          ls -la boot-files/
          
          # 验证boot包文件完整性
          echo "🔍 Verifying boot package integrity..."
          if [ -f "boot-files/rootfs-uuid.txt" ]; then
            ROOTFS_UUID=$(cat boot-files/rootfs-uuid.txt)
            echo "ROOTFS_UUID=$ROOTFS_UUID" >> $GITHUB_ENV
            echo "✅ RootFS UUID: $ROOTFS_UUID"
          else
            echo "❌ RootFS UUID file not found in boot package"
            exit 1
          fi
          
          if [ -f "boot-files/vmlinuz" ]; then
            echo "✅ vmlinuz file found and accessible"
            file "boot-files/vmlinuz"
            ls -lh "boot-files/vmlinuz"
          else
            echo "❌ vmlinuz file not found in boot package"
            exit 1
          fi
          
          if [ -f "boot-files/initrd.img" ]; then
            echo "✅ initrd.img file found and accessible"
            file "boot-files/initrd.img"
            ls -lh "boot-files/initrd.img"
          else
            echo "❌ initrd.img file not found in boot package"
            exit 1
          fi
          
          echo "✅ Boot package integrity verified successfully"
        else
          echo "❌ No boot package found"
          exit 1
        fi
        
        # 下载rootfs镜像（用于验证和参考）
        echo "📥 Downloading rootfs image..."
        gh release download $RELEASE_TAG \
          --pattern "root-*$DISTRIBUTION*.zip" \
          --repo ${{ github.repository }} || {
          echo "⚠️ Failed to download zip from release, trying img file"
          # 如果zip不存在，下载img文件
          gh release download $RELEASE_TAG \
            --pattern "root-*$DISTRIBUTION*.img" \
            --repo ${{ github.repository }} || {
            echo "⚠️ Failed to download rootfs from release, trying artifacts"
            # 从工作流产物下载
            gh run download --name "xiaomi-raphael-rootfs_${DISTRIBUTION}_${KERNEL_VERSION}" || {
              echo "❌ No rootfs image found"
              exit 1
            }
          }
        }
        
        # 如果是zip文件，则解压
        if ls root-*.zip 1> /dev/null 2>&1; then
          echo "🗜️ Extracting rootfs zip file..."
          ZIP_FILE=$(ls root-*.zip | head -1)
          unzip "$ZIP_FILE"
          echo "✅ Extracted: $ZIP_FILE"
          # 删除zip文件以节省空间
          rm "$ZIP_FILE"
        fi
        
        ROOTFS_IMG=$(ls root-*.img | head -1)
        if [ -z "$ROOTFS_IMG" ]; then
          echo "⚠️ No rootfs image found after extraction"
        else
          echo "✅ Rootfs image: $ROOTFS_IMG"
        fi
        
    - name: Build boot image using script
      run: |
        echo "🔧 Building boot image using raphael-boot_build.sh..."
        
        # 给脚本执行权限
        chmod +x raphael-boot_build.sh
        
        # 执行boot镜像构建脚本
        ./raphael-boot_build.sh \
          --kernel-version "$KERNEL_VERSION" \
          --distribution "$DISTRIBUTION" \
          --rootfs-image "$ROOTFS_IMG" \
          --output "xiaomi-k20pro-boot-${DISTRIBUTION}-${KERNEL_VERSION}.img"
        
        # Extract ROOTFS_UUID from script output
        if grep -q "ROOTFS_UUID" output/boot/boot-image-info.txt; then
          ROOTFS_UUID=$(grep "ROOTFS_UUID" output/boot/boot-image-info.txt | cut -d'=' -f2)
          echo "ROOTFS_UUID=$ROOTFS_UUID" >> $GITHUB_ENV
        fi
        
        echo "✅ Boot image built successfully"
        
    - name: Verify boot image
      run: |
        echo "🔍 Verifying boot image..."
        
        # Check if boot image exists
        BOOT_IMG=$(ls xiaomi-k20pro-boot-*.img 2>/dev/null | head -1)
        if [ -z "$BOOT_IMG" ]; then
          echo "❌ Boot image not found"
          exit 1
        fi
        
        echo "✅ Boot image: $BOOT_IMG"
        echo "📊 Boot image details:"
        ls -lh "$BOOT_IMG"
        file "$BOOT_IMG"
        
        # Verify image size (should be around 256MB for boot partition)
        IMG_SIZE=$(stat -c%s "$BOOT_IMG")
        MIN_SIZE=$((200 * 1024 * 1024))  # 200MB
        MAX_SIZE=$((500 * 1024 * 1024))  # 500MB
        if [ "$IMG_SIZE" -ge "$MIN_SIZE" ] && [ "$IMG_SIZE" -le "$MAX_SIZE" ]; then
          echo "✅ Boot image size is within expected range"
        else
          echo "⚠️ Boot image size ($IMG_SIZE bytes) is outside expected range ($MIN_SIZE - $MAX_SIZE bytes)"
        fi
        
        # Try to mount and check content
        echo "🔍 Checking boot image content..."
        mkdir -p /tmp/boot-mount
        sudo mount -o loop "$BOOT_IMG" /tmp/boot-mount
        
        # Check essential boot files
        essential_files=("/tmp/boot-mount/EFI/boot/bootaa64.efi" "/tmp/boot-mount/loader/entries/ubuntu.conf" "/tmp/boot-mount/linux.efi" "/tmp/boot-mount/initramfs")
        
        for file in "${essential_files[@]}"; do
          if [ -f "$file" ]; then
            echo "✅ Found: $file"
          else
            echo "❌ Missing: $file"
          fi
        done
        
        # Check loader configuration
        if [ -f "/tmp/boot-mount/loader/loader.conf" ]; then
          echo "📄 Loader configuration:"
          cat /tmp/boot-mount/loader/loader.conf
        fi
        
        # Check Ubuntu entry
        if [ -f "/tmp/boot-mount/loader/entries/ubuntu.conf" ]; then
          echo "📄 Ubuntu boot entry:"
          cat /tmp/boot-mount/loader/entries/ubuntu.conf
          
          # Verify kernel version in entry
          if grep -q "$KERNEL_VERSION" /tmp/boot-mount/loader/entries/ubuntu.conf; then
            echo "✅ Kernel version in boot entry matches: $KERNEL_VERSION"
          else
            echo "⚠️ Kernel version in boot entry does not match"
          fi
        fi
        
        # Unmount
        sudo umount /tmp/boot-mount
        rmdir /tmp/boot-mount
        
        echo "✅ Boot image verification completed"
        
    - name: Upload boot image to GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ env.KERNEL_VERSION }}
        name: "Boot Image ${{ env.KERNEL_VERSION }} for Xiaomi Raphael"
        body: |
          # Boot Image ${{ env.KERNEL_VERSION }} for Xiaomi Raphael
          
          ## Build Information
          - Distribution: ${{ env.DISTRIBUTION }}
          - Kernel Version: ${{ env.KERNEL_VERSION }}
          - Architecture: ARM64
          - Target Device: Xiaomi Raphael (K20 Pro)
          - Build Date: ${{ github.event.repository.created_at }}
          - Build ID: ${{ github.run_id }}
          
          ## Generated Files
          - **Boot Image**: Complete bootable image (xiaomi-k20pro-boot-*.img)
          - **Kernel Files**: vmlinuz and initrd.img included in the image
          - **EFI System**: UEFI bootloader configuration
          - **RootFS UUID**: Unique identifier for the root filesystem
          
          ## Usage
          
          ### Flashing the Boot Image:
          ```bash
          # Use fastboot to flash the boot image
          fastboot flash boot xiaomi-k20pro-boot-${{ env.DISTRIBUTION }}-${{ env.KERNEL_VERSION }}.img
          fastboot reboot
          ```
          
          ### Verification:
          After flashing, verify the boot image contains the correct files:
          ```bash
          # Mount the boot image to check contents
          mkdir /tmp/boot-mount
          sudo mount -o loop xiaomi-k20pro-boot-${{ env.DISTRIBUTION }}-${{ env.KERNEL_VERSION }}.img /tmp/boot-mount
          ls -la /tmp/boot-mount/
          sudo umount /tmp/boot-mount
          ```
          
          ## Changelog
          - Updated kernel to ${{ env.KERNEL_VERSION }}
          - Distribution: ${{ env.DISTRIBUTION }}
          - Improved boot configuration and EFI setup
        files: xiaomi-k20pro-boot-${{ env.DISTRIBUTION }}-${{ env.KERNEL_VERSION }}.img
        draft: false
        prerelease: false
        
    - name: Display build summary
      run: |
        echo "🎉 Boot image creation completed successfully!"
        echo "📦 Distribution: $DISTRIBUTION"
        echo "🔧 Kernel Version: $KERNEL_VERSION"
        echo "🔑 RootFS UUID: ${{ env.ROOTFS_UUID }}"
        echo "📤 Boot image uploaded to GitHub Release: $RELEASE_TAG"
        echo "📊 Final boot image:"
        ls -lh xiaomi-k20pro-boot-*.img