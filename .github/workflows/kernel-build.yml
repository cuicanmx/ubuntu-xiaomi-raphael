name: Kernel Build and Release

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Kernel version and release tag (e.g., 6.18)'
        required: true
        type: string
        default: '6.18'
      upload_to_release:
        description: 'Upload kernel packages to GitHub Release'
        required: false
        type: boolean
        default: true

  jobs:
    build-kernel:
      name: Build Kernel Packages
      runs-on: ubuntu-latest
      
      steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            bison \
            flex \
            libssl-dev \
            bc \
            git \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu
            
      - name: Build kernel packages
        run: |
          chmod +x ./raphael-kernel_build.sh
          ./raphael-kernel_build.sh "${{ github.event.inputs.kernel_version }}"
          
      - name: Verify kernel packages
        run: |
          echo "🔍 Verifying generated kernel packages:"
          ls -la *.deb || echo "No DEB packages found"
          
      - name: Upload kernel packages to release
        if: github.event.inputs.upload_to_release == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.event.inputs.kernel_version }}
          name: "Kernel ${{ github.event.inputs.kernel_version }} for Xiaomi Raphael"
          body: "Kernel ${{ github.event.inputs.kernel_version }} for Xiaomi Raphael"
          files: |
            linux-xiaomi-raphael_*.deb
            firmware-xiaomi-raphael_*.deb
            alsa-xiaomi-raphael_*.deb
          draft: false
          prerelease: false
          
      - name: Upload kernel packages as artifacts
        if: github.event.inputs.upload_to_release != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: kernel-packages-${{ github.event.inputs.kernel_version }}
          path: |
            linux-xiaomi-raphael_*.deb
            firmware-xiaomi-raphael_*.deb
            alsa-xiaomi-raphael_*.deb
          retention-days: 30